# ============================================
# EXAMPLE ENVIRONMENT CONFIGURATION
# Copy this file to .env and fill in your values
# NEVER commit the actual .env file!
# ============================================

# CORS Configuration
CORS_ORIGIN=http://localhost:3001

# Authentication Secret (Generate a secure random string)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here-min-32-chars

# Better Auth URL
BETTER_AUTH_URL=http://localhost:3005

# Database Configuration (PostgreSQL/Supabase)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?pgbouncer=true
DATABASE_URL=postgresql://user:password@host:port/database?pgbouncer=true

# ============================================
# LDAP/Active Directory Configuration
# ============================================

# LDAP Server URL (use ldaps:// for secure connection)
LDAP_URL=ldap://your-ldap-server:389

# LDAP Base DN (Distinguished Name)
LDAP_BASE_DN=DC=example,DC=com

# LDAP Admin/Service Account DN
LDAP_ADMIN_DN=CN=ServiceAccount,OU=Service,DC=example,DC=com

# LDAP Admin Password (Use a service account, not personal account)
LDAP_ADMIN_PASSWORD="your-service-account-password"

# LDAP Search Filter (for finding users)
LDAP_SEARCH_FILTER=(&(objectClass=user)(sAMAccountName={{username}}))

# ============================================
# Microsoft 365 OAuth Configuration
# ============================================

# Register your app at: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
MICROSOFT_CLIENT_ID=your-azure-app-client-id
MICROSOFT_CLIENT_SECRET=your-azure-app-client-secret
MICROSOFT_TENANT_ID=your-tenant-id-or-common
MICROSOFT_REDIRECT_URI=http://localhost:3005/api/auth/microsoft/callback

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3001